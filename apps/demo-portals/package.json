{
  "name": "@trustmode/demo-portals",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --port 4175",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b",
    "lint": "tsc --noEmit",
    "test": "echo \"Portal behavior is covered by web-demo E2E\""
  },
  "dependencies": { "@trustmode/core": "workspace:*" },
  "devDependencies": { "typescript": "^7.0.2", "vite": "^7.0.4" }
}
